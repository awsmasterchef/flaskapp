version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - zip -r my-deployment-package.zip .
artifacts:
  files:
    - my-deployment-package.zip
